[project]
name = "claude-md-discovery-extended"
version = "0.2.1"
description = "Extends CLAUDE.md auto-discovery to directories outside the project tree"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
